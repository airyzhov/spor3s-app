# 🚀 Быстрый перезапуск Mini App после перезагрузки VPS

## Выполните эти команды на VPS:

```bash
cd /var/www/spor3s-app

# 1. Остановить старый процесс
pm2 stop spor3s-nextjs 2>/dev/null || true
pm2 delete spor3s-nextjs 2>/dev/null || true

# 2. Освободить порт 3000 если занят
kill -9 $(lsof -t -i:3000) 2>/dev/null || true
sleep 2

# 3. Запустить Next.js приложение
pm2 start npm --name "spor3s-nextjs" -- start

# 4. Сохранить конфигурацию PM2
pm2 save

# 5. Перезапустить Nginx
systemctl restart nginx

# 6. Проверить статус
pm2 status
pm2 logs spor3s-nextjs --lines 20 --nostream
```

## Или используйте скрипт (если загружен):

```bash
cd /var/www/spor3s-app
chmod +x restart_mini_app.sh
./restart_mini_app.sh
```

## Проверка работы:

1. **Проверить статус PM2:**
   ```bash
   pm2 status
   ```
   Должен быть процесс `spor3s-nextjs` со статусом `online`

2. **Проверить логи:**
   ```bash
   pm2 logs spor3s-nextjs
   ```
   Не должно быть ошибок

3. **Проверить локально:**
   ```bash
   curl http://localhost:3000
   ```
   Должен вернуть HTML страницу

4. **Проверить через браузер:**
   - Откройте https://ai.spor3s.ru
   - Должна загрузиться главная страница

## Если не работает:

1. **Проверить, что приложение запущено:**
   ```bash
   pm2 list
   ```

2. **Проверить порт 3000:**
   ```bash
   netstat -tlnp | grep 3000
   # или
   lsof -i :3000
   ```

3. **Проверить логи Nginx:**
   ```bash
   tail -f /var/log/nginx/error.log
   ```

4. **Проверить конфигурацию Nginx:**
   ```bash
   nginx -t
   ```

5. **Перезапустить все:**
   ```bash
   pm2 restart all
   systemctl restart nginx
   ```

