# Настройка автоматического деплоя через GitHub Actions

## 📋 Обзор

GitHub Actions автоматически деплоит код на VPS при каждом push в ветку `main`.

## 🔐 Настройка Secrets в GitHub

### Шаг 1: Откройте настройки репозитория

1. Перейдите на https://github.com/airyzhov/spor3s-app
2. Нажмите **Settings** → **Secrets and variables** → **Actions**
3. Нажмите **New repository secret**

### Шаг 2: Добавьте следующие Secrets

#### 1. VPS_HOST
- **Name:** `VPS_HOST`
- **Value:** `185.166.197.49`

#### 2. VPS_USER
- **Name:** `VPS_USER`
- **Value:** `root`

#### 3. VPS_SSH_KEY
- **Name:** `VPS_SSH_KEY`
- **Value:** Содержимое приватного SSH ключа

**Как получить SSH ключ:**

**Вариант A: Использовать существующий ключ**
```powershell
# В PowerShell на Windows
Get-Content $env:USERPROFILE\.ssh\id_ed25519
```

Скопируйте весь вывод (включая строки `-----BEGIN OPENSSH PRIVATE KEY-----` и `-----END OPENSSH PRIVATE KEY-----`)

**Вариант B: Создать новый ключ специально для GitHub Actions**
```bash
# На вашем компьютере
ssh-keygen -t ed25519 -C "github-actions" -f ~/.ssh/github_actions_key

# Покажите публичный ключ
cat ~/.ssh/github_actions_key.pub
```

Затем добавьте публичный ключ на VPS:
```bash
# На VPS (через другой доступ или попросите администратора)
mkdir -p ~/.ssh
echo "ВАШ_ПУБЛИЧНЫЙ_КЛЮЧ" >> ~/.ssh/authorized_keys
chmod 600 ~/.ssh/authorized_keys
chmod 700 ~/.ssh
```

И используйте приватный ключ (`~/.ssh/github_actions_key`) как значение для `VPS_SSH_KEY`

## 📝 Проверка настройки

После добавления всех secrets:

1. Сделайте любой commit и push в ветку `main`:
   ```bash
   git add .
   git commit -m "test: проверка автоматического деплоя"
   git push origin main
   ```

2. Проверьте статус деплоя:
   - Перейдите на https://github.com/airyzhov/spor3s-app/actions
   - Откройте последний workflow run
   - Проверьте логи выполнения

## 🔍 Что делает workflow

1. **Checkout code** - получает код из репозитория
2. **Deploy to VPS** - подключается к VPS и выполняет:
   - Обновление кода из GitHub (`git pull`)
   - Проверка и копирование `.env` файлов
   - Установка зависимостей (`npm ci`)
   - Сборка Next.js (`npm run build`)
   - Перезапуск PM2 процессов (`pm2 restart`)

## 🛠 Ручной запуск деплоя

Если нужно запустить деплой вручную:

1. Перейдите на https://github.com/airyzhov/spor3s-app/actions
2. Выберите workflow **Deploy to VPS**
3. Нажмите **Run workflow**
4. Выберите ветку `main`
5. Нажмите **Run workflow**

## 🐛 Решение проблем

### Ошибка: "Permission denied (publickey)"

**Причина:** SSH ключ не добавлен на VPS или неверный ключ в secrets.

**Решение:**
1. Проверьте, что публичный ключ добавлен в `~/.ssh/authorized_keys` на VPS
2. Убедитесь, что приватный ключ в secrets соответствует публичному на VPS
3. Проверьте права доступа на VPS:
   ```bash
   chmod 700 ~/.ssh
   chmod 600 ~/.ssh/authorized_keys
   ```

### Ошибка: "Host key verification failed"

**Решение:** Добавьте VPS в known_hosts GitHub Actions (автоматически обрабатывается через `appleboy/ssh-action`)

### Ошибка: "npm: command not found"

**Решение:** Установите Node.js на VPS:
```bash
curl -fsSL https://deb.nodesource.com/setup_20.x | bash -
apt-get install -y nodejs
```

### Ошибка: "pm2: command not found"

**Решение:** Установите PM2 на VPS:
```bash
npm install -g pm2
```

## 📊 Мониторинг деплоя

После успешного деплоя проверьте:

```bash
# На VPS
cd /var/www/spor3s-app
pm2 status
pm2 logs
```

## 🔄 Откат изменений

Если деплой прошел неудачно:

1. Откатите изменения в Git:
   ```bash
   git revert HEAD
   git push origin main
   ```

2. Или вручную на VPS:
   ```bash
   cd /var/www/spor3s-app
   git reset --hard HEAD~1
   pm2 restart all
   ```

## 📚 Дополнительные ресурсы

- [GitHub Actions Documentation](https://docs.github.com/en/actions)
- [SSH Action Documentation](https://github.com/appleboy/ssh-action)
- [PM2 Documentation](https://pm2.keymetrics.io/docs/usage/quick-start/)

