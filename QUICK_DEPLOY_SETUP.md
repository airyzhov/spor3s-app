# 🚀 Быстрая настройка автоматического деплоя

## За 5 минут до автоматического деплоя

### Шаг 1: Генерация SSH ключа (1 мин)

```powershell
.\generate_github_actions_key.ps1
```

Скопируйте **публичный** и **приватный** ключи из вывода.

### Шаг 2: Добавление ключа на VPS (2 мин)

**Если есть доступ к VPS:**
```bash
ssh root@185.166.197.49
echo 'ВАШ_ПУБЛИЧНЫЙ_КЛЮЧ' >> ~/.ssh/authorized_keys
chmod 600 ~/.ssh/authorized_keys
exit
```

**Если нет доступа:** Попросите администратора VPS добавить публичный ключ в `~/.ssh/authorized_keys`

### Шаг 3: Настройка GitHub Secrets (2 мин)

1. Откройте https://github.com/airyzhov/spor3s-app/settings/secrets/actions
2. Нажмите **New repository secret** и добавьте:

   **VPS_HOST:**
   ```
   185.166.197.49
   ```

   **VPS_USER:**
   ```
   root
   ```

   **VPS_SSH_KEY:**
   ```
   -----BEGIN OPENSSH PRIVATE KEY-----
   [весь приватный ключ из шага 1]
   -----END OPENSSH PRIVATE KEY-----
   ```

### Шаг 4: Тест деплоя

```bash
git add .
git commit -m "test: проверка автоматического деплоя"
git push origin main
```

Проверьте результат: https://github.com/airyzhov/spor3s-app/actions

## ✅ Готово!

Теперь каждый push в `main` автоматически деплоит код на VPS.

## 📋 Что происходит при деплое

1. ✅ Обновление кода из GitHub
2. ✅ Установка зависимостей
3. ✅ Сборка Next.js
4. ✅ Перезапуск PM2 процессов

## 🐛 Проблемы?

См. подробную инструкцию: `GITHUB_ACTIONS_SETUP.md`

