# 🚀 Запуск приложения на VPS

## Текущая ситуация

- ✅ Домен `ai.spor3s.ru` настроен в nginx
- ✅ Nginx работает (возвращает 502 вместо timeout)
- ❌ Next.js приложение не запущено (порт 3000 недоступен)
- ❌ PM2 процессы не запущены

## Быстрый запуск

### Вариант 1: Через GitHub Actions (после настройки secrets)

После настройки GitHub Secrets деплой запустится автоматически при push в `main`.

### Вариант 2: Вручную на VPS

```bash
# Подключитесь к VPS
ssh root@185.166.197.49

# Перейдите в директорию проекта
cd /var/www/spor3s-app

# Если репозитория нет - клонируйте
if [ ! -d .git ]; then
    git clone https://github.com/airyzhov/spor3s-app.git .
fi

# Обновите код
git pull origin main

# Проверьте .env файл
if [ ! -f .env ]; then
    if [ -f .env.vps ]; then
        cp .env.vps .env
    elif [ -f env-production ]; then
        cp env-production .env
    fi
fi

# Установите зависимости
npm install

# Соберите приложение
npm run build

# Запустите через PM2
pm2 start ecosystem.config.js
pm2 save

# Проверьте статус
pm2 status
```

## Проверка работы

После запуска проверьте:

```bash
# Статус PM2 процессов
pm2 status

# Логи
pm2 logs

# Проверка порта
netstat -tlnp | grep 3000

# Проверка через curl
curl http://localhost:3000
```

## Проверка домена

После запуска приложения домен должен быть доступен:
- https://ai.spor3s.ru

## Решение проблем

### Порт 3000 занят другим процессом

```bash
# Найдите процесс на порту 3000
lsof -i :3000
# или
netstat -tlnp | grep 3000

# Остановите процесс
pm2 delete all
# или
kill -9 <PID>
```

### PM2 не запускается

```bash
# Установите PM2 глобально
npm install -g pm2

# Проверьте ecosystem.config.js
cat ecosystem.config.js
```

### Ошибки при сборке

```bash
# Очистите кэш и пересоберите
rm -rf .next node_modules
npm install
npm run build
```

### Nginx не проксирует

```bash
# Проверьте конфигурацию nginx
cat /etc/nginx/sites-available/ai.spor3s.ru
# или
cat /etc/nginx/conf.d/spor3s-app.conf

# Перезагрузите nginx
nginx -t
systemctl reload nginx
```

## Автоматический запуск при перезагрузке

```bash
# Настройте автозапуск PM2
pm2 startup
pm2 save
```

## Мониторинг

```bash
# Просмотр логов в реальном времени
pm2 logs

# Мониторинг ресурсов
pm2 monit

# Статус всех процессов
pm2 status
```

