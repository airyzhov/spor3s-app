# 🚀 Деплой всех агентов spor3s на VPS

## 📋 Три агента системы

### 1. 🌐 **spor3s-nextjs** - Mini App (Next.js)
- Веб-интерфейс для заказов
- AI-чат в Mini App
- Корзина и оформление заказов

### 2. 🤖 **spor3s-bot** - Telegram Bot API
- Прием заказов через @spor3s_bot
- Обработка команд: /start, /my_coins, /verify_youtube, /help
- AI-чат в Telegram
- Отправка уведомлений менеджеру

### 3. 📨 **spor3z-client** - Telegram Client API  
- Исходящие напоминания пользователям
- Ежедневные уведомления в 07:00
- Управление согласием на напоминания
- Мониторинг активных курсов

---

## 🎯 Быстрый старт на VPS

### Вариант 1: Автоматический деплой через SSH

```bash
# Деплой с локальной машины на VPS
chmod +x deploy-vps-complete.sh
./deploy-vps-complete.sh
```

### Вариант 2: Запуск локально на VPS

```bash
# На VPS
cd /var/www/spor3s-app
node start-all-agents.js
```

---

## 📊 Проверка статуса всех агентов

```bash
# Локально или на VPS
node check-all-agents.js
```

Вывод:
```
🔍 Проверка статуса всех агентов spor3s...

🌐 Mini App (Next.js) - Интерфейс для заказов и AI-чат
  ✅ Статус: online
  📊 Память: 245 MB, CPU: 0.1%
  🔄 Перезапусков: 0
  🌐 HTTP: ✅ (200)

🤖 spor3s_bot - Прием заказов через бот
  ✅ Статус: online
  📊 Память: 89 MB, CPU: 0%
  🔄 Перезапусков: 0

📨 spor3z - Исходящие напоминания
  ✅ Статус: online
  📊 Память: 67 MB, CPU: 0%
  🔄 Перезапусков: 0

═══════════════════════════════════════
📊 Статус: 3/3 агентов активны
✅ Все системы работают нормально!
```

---

## ⚙️ Управление PM2

### Просмотр всех процессов
```bash
pm2 status
```

### Логи конкретного агента
```bash
pm2 logs spor3s-nextjs     # Next.js приложение
pm2 logs spor3s-bot        # Telegram Bot API
pm2 logs spor3z-client     # Telegram Client API
```

### Перезапуск
```bash
pm2 restart spor3s-bot     # Перезапуск конкретного
pm2 restart all            # Перезапуск всех
```

### Остановка/Запуск
```bash
pm2 stop spor3z-client
pm2 start spor3z-client
```

### Удаление процесса
```bash
pm2 delete spor3s-bot
```

---

## 🔧 Конфигурация окружения

### Обязательные переменные в `.env`:

```bash
# Supabase
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Telegram Bot API (@spor3s_bot)
TELEGRAM_BOT_TOKEN=your-bot-token
BOT_USERNAME=spor3s_bot
MANAGER_CHAT_ID=your-telegram-id

# Telegram Client API (spor3z напоминания)
TELEGRAM_API_ID=your-api-id
TELEGRAM_API_HASH=your-api-hash
TELEGRAM_SESSION_STRING=your-session-string

# AI
OPENROUTER_API_KEY=your-openrouter-key

# App
NEXT_PUBLIC_BASE_URL=https://ai.spor3s.ru
NODE_ENV=production
```

---

## 🐛 Решение проблем

### spor3s-bot не запускается

```bash
# Проверить логи
pm2 logs spor3s-bot --lines 50

# Проверить переменные окружения
cat tg-bot/.env

# Убедиться что есть TELEGRAM_BOT_TOKEN
grep TELEGRAM_BOT_TOKEN tg-bot/.env
```

### spor3z-client ошибка "not authorized"

```bash
# Требуется сгенерировать TELEGRAM_SESSION_STRING
# Запустите локально:
node generate-session-simple.js

# Скопируйте полученный SESSION_STRING в .env
```

### Next.js ошибка при сборке

```bash
# Пересоздать сборку
rm -rf .next
npm run build
pm2 restart spor3s-nextjs
```

---

## 📡 Nginx Proxy Manager

На VPS (185.166.197.49) используется Nginx Proxy Manager.

### Настройка хоста для Mini App:

1. Откройте Nginx Proxy Manager Admin панель
2. Добавьте Proxy Host:
   - **Domain**: `ai.spor3s.ru`
   - **Scheme**: `http`
   - **Forward Hostname**: `localhost`
   - **Forward Port**: `3000`
   - **SSL**: запросить Let's Encrypt сертификат

---

## 🔄 Автозапуск при перезагрузке VPS

```bash
# На VPS
pm2 startup
# Выполнить команду которую выдаст PM2

pm2 save
```

---

## 📞 Тестирование агентов

### Тест 1: Mini App
```bash
curl http://localhost:3000
# Должен вернуть HTML
```

### Тест 2: Telegram Bot
Отправьте `/help` боту @spor3s_bot в Telegram

### Тест 3: Telegram Client
```bash
# На VPS
cd /var/www/spor3s-app
node tg-client/client.js send-reminder YOUR_TELEGRAM_ID "Тест напоминания"
```

---

## 📚 Дополнительные команды

### Мониторинг в реальном времени
```bash
pm2 monit
```

### Экспорт логов
```bash
pm2 logs --lines 1000 > all-logs.txt
```

### Обновление кода без остановки
```bash
# Загрузить новый код на VPS, затем:
pm2 reload all
```

---

## ✅ Чеклист после деплоя

- [ ] Все три агента online в `pm2 status`
- [ ] Логи без критических ошибок `pm2 logs`
- [ ] Mini App отвечает на http://185.166.197.49:3000
- [ ] @spor3s_bot отвечает на команды в Telegram
- [ ] spor3z-client подключен (логи показывают "Telegram client is running")
- [ ] Настроен автозапуск через `pm2 startup`
- [ ] Nginx Proxy Manager настроен для ai.spor3s.ru

---

**Готово! 🎉 Все три агента работают синхронно.**

