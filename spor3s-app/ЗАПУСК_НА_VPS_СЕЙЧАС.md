# 🚀 ЗАПУСК БОТА НА VPS ПРЯМО СЕЙЧАС

**Дата:** ${new Date().toLocaleString('ru-RU')}  
**VPS:** http://185.166.197.49/  
**Домен:** https://ai.spor3s.ru

---

## 🎯 ПРОБЛЕМА

Бот @spor3s_bot молчит потому что:
- ❌ **Процесс НЕ запущен** на VPS
- ❌ **Локальные попытки** не работают
- ❌ **Нужно запустить на сервере**

---

## 🚀 РЕШЕНИЕ: ЗАПУСК НА VPS

### Шаг 1: Подключитесь к VPS

```bash
ssh root@185.166.197.49
```

### Шаг 2: Перейдите в директорию проекта

```bash
cd /root/spor3s-app/spor3s-app
# или
cd /var/www/spor3s-app/spor3s-app
# или где установлен проект
```

### Шаг 3: Проверьте файлы

```bash
ls -la final-bot-fix.js
ls -la env.local
```

### Шаг 4: Обновите OpenRouter токен

```bash
nano env.local
```

Найдите строку:
```env
OR_TOKEN=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
```

Замените на новый токен:
```env
OR_TOKEN=ваш_новый_токен_здесь
```

**Получите новый токен на:** https://openrouter.ai/

### Шаг 5: Запустите бота через PM2

```bash
# Остановите старые процессы (если есть)
pm2 stop all
pm2 delete all

# Запустите исправленного бота
pm2 start final-bot-fix.js --name "spor3s-bot-fixed"

# Сохраните конфигурацию
pm2 save

# Настройте автозапуск
pm2 startup
```

### Шаг 6: Проверьте статус

```bash
# Посмотрите список процессов
pm2 list

# Посмотрите логи
pm2 logs spor3s-bot-fixed

# Посмотрите последние логи
pm2 logs spor3s-bot-fixed --lines 50
```

---

## 🔧 АЛЬТЕРНАТИВНЫЙ ЗАПУСК

### Если PM2 не установлен:

```bash
# Установите PM2
npm install -g pm2

# Или запустите напрямую
node final-bot-fix.js
```

### Если файлы не найдены:

```bash
# Найдите проект
find / -name "final-bot-fix.js" 2>/dev/null

# Или создайте файл заново
nano final-bot-fix.js
# Скопируйте содержимое из локального файла
```

---

## 🧪 ТЕСТИРОВАНИЕ

### После запуска на VPS:

1. **Откройте Telegram**
2. **Найдите @spor3s_bot**
3. **Отправьте сообщения:**
   - `/start`
   - `привет`
   - `есть ежовик?`

### Проверьте логи:

```bash
pm2 logs spor3s-bot-fixed --lines 20
```

**Ожидаемые логи:**
```
🚀 Исправленный бот запущен!
📱 Отправьте боту сообщение для тестирования
📱 Получено: "привет" от Алексей
✅ Ответ отправлен пользователю
```

---

## 🚨 ЕСЛИ БОТ ВСЕ ЕЩЕ МОЛЧИТ

### Проверьте:

1. **Процесс запущен:**
   ```bash
   pm2 list
   ```

2. **Логи без ошибок:**
   ```bash
   pm2 logs spor3s-bot-fixed
   ```

3. **Токены действительны:**
   ```bash
   grep OR_TOKEN env.local
   ```

4. **AI API работает:**
   ```bash
   curl -X POST https://ai.spor3s.ru/api/ai \
     -H "Content-Type: application/json" \
     -d '{"message":"тест","source":"test"}'
   ```

### Перезапустите бота:

```bash
pm2 restart spor3s-bot-fixed
pm2 logs spor3s-bot-fixed
```

---

## 📊 ОЖИДАЕМЫЙ РЕЗУЛЬТАТ

После запуска на VPS бот должен:

✅ **Отвечать на все сообщения**  
✅ **Давать рекомендации по продуктам**  
✅ **Работать с AI API**  
✅ **Сохранять историю в Supabase**  

---

## 🎉 КОМАНДЫ ДЛЯ БЫСТРОГО ЗАПУСКА

```bash
# 1. Подключиться к VPS
ssh root@185.166.197.49

# 2. Перейти в проект
cd /root/spor3s-app/spor3s-app

# 3. Обновить токен (если нужно)
nano env.local

# 4. Запустить бота
pm2 start final-bot-fix.js --name "spor3s-bot-fixed"
pm2 save

# 5. Проверить
pm2 list
pm2 logs spor3s-bot-fixed
```

---

**Дата создания:** ${new Date().toLocaleString('ru-RU')}  
**Статус:** Готов к запуску на VPS

_После выполнения команд бот будет работать на сервере!_
